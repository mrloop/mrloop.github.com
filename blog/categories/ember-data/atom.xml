<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Ember Data | mrloop]]></title>
  <link href="http://blog.mrloop.com/blog/categories/ember-data/atom.xml" rel="self"/>
  <link href="http://blog.mrloop.com/"/>
  <updated>2013-04-02T14:23:08+01:00</updated>
  <id>http://blog.mrloop.com/</id>
  <author>
    <name><![CDATA[mrloop]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Ember Data and the Meetup API]]></title>
    <link href="http://blog.mrloop.com/blog/2013/04/02/ember-data-and-the-meetup-api.markdown/"/>
    <updated>2013-04-02T15:45:00+01:00</updated>
    <id>http://blog.mrloop.com/blog/2013/04/02/ember-data-and-the-meetup-api.markdown</id>
    <content type="html"><![CDATA[<p>I'm a member of the highland web group and they were looking for somebody to
create a website for the group. Alisdair and myself were interested in doing
so. The group's organized via <a href="http://www.meetup.com/Highland-Web-Group/">meetup.com</a> and they have
a fairly comprehensive <a href="http://www.meetup.com/meetup_api/">API</a> for accessing
their data. The Meetup data should always be up to date so instead of burdening
somebody with the task of updating meetup with current information and our own
website with an alternative CMS the best option was to populate
the <a href="http://highlandwebgroup.github.com">highlandweb group</a> with meet up data dynamically.
To do this I decided to use <a href="http://emberjs.com">Ember</a> and <a href="http://emberjs.com/guides/models/">Ember Data</a>.</p>

<p>Ember is a MVC javascript framework for creating web applications, Ember Data is a data persistence framework for Ember.
Ember Data provides a central Data Store and Adapters to talk to different data sources.
Ember and Ember Data believe in convention over configuration, the framework will do a lot of work for you if you follow the conventions.
This can make things a little tricky if you step away from those conventions.</p>

<p>The meetup API is not 100% consistent in how it defines object ids e.g. <a href="#rsvps">RSVPs</a>
have their ids declared as 'rsvp_id' whilst most other models simply use 'id'.
The RSVPs also use a '-1' id as a flag indicating the RSVP is a recurring rsvp
from the events organizer. Also nested objects aren't consistent or always
complete e.g. <a href="#groups">Groups</a>.</p>

<p>The inconsistent JSON structure returned by the Meetup API make it difficult to write an ember data adapter for it.
Luckily ember data have recently addressed this issue with the <a href="http://emberjs.com/blog/2013/03/22/stabilizing-ember-data.html">Basic Adapter</a>
letting you write a sync object for specific models in your API so you can work around the inconsistencies.</p>

<h2>Sync Object</h2>

<p>I define a function <em>meetupSync</em> which returns a generic sync object.</p>

<p>``` js MeetupAdapter.js https://github.com/HighlandWebGroup/highlandwebgroup.github.com/blob/master/app/lib/MeetupAdapter.js
App.meetupSync = function(type){
  sync = {</p>

<pre><code>munge: function(json){json},
findHasManyQueryOptions: {},
base_url:"https://api.meetup.com/2",
find: function(id, process){
  q = {};
  q[this.type+"_id"] = id;
  _munge = this.munge;
  this._query(q, function(result){
    process(result.results.length &gt; 0 ? result.results[0]  : [])
      .munge(_munge)
      .load();
  });
},
query: function(query, process){
  _munge = this.munge;
  this._query(query, function(result){
    process(result.results || [])
      .munge(_munge)
      .load();
  })
},
findHasMany: function(record, options, processor){
  relationship_model = record.constructor.typeForRelationship(options.relationship)
  q = {};
  q[record.constructor.sync.type+"_id"] = record.id;
  relationship_model.sync.query(Ember.$.extend({},q, this.findHasManyQueryOptions), processor)
},
_query: _.rateLimit(function(query, processResult){
  Ember.assert("Must specify type", this.type);
  App.ajax(this.base_url+"/"+this.type+"s", "GET", {data: query}).then(processResult);
}, 100),
</code></pre>

<p>  }
  sync_copy = Ember.$.extend(true, {}, sync)
  sync_copy.type = type;
  return sync_copy;
}
```</p>

<p>This is a generalized sync object for the meetup API that I can customise for each model
as needed. I define 3 functions for finding records from the API.</p>

<ul>
<li><em>find</em> for finding records by Id</li>
<li><em>query</em> for finding records by other parameters</li>
<li><em>findHasMany</em> for finding related records.</li>
</ul>


<p>In my implementation <em>find</em> and <em>findHasMany</em> both use <em>query</em> to find records.
The <em>query</em> function takes 2 arguments, 'query' e.g.
<code>js
{ status: 'upcoming' }
</code>
and a a function 'process' with we use to load the JSON from our request into the Data Store after we have done our own
processing with our <em>munge</em> function. The query function uses our private
function <em>_query</em> which is <a href="https://gist.github.com/mattheworiordan/1084831">rate limited</a> to stop meetup API restricting access
for too frequent API calls.</p>

<h2>Models</h2>

<h3>Event Models</h3>

<p>``` js event.js https://github.com/HighlandWebGroup/highlandwebgroup.github.com/blob/master/app/lib/models/event.js
var attr = DS.attr;</p>

<p>App.Event = DS.Model.extend({</p>

<p>  rsvps: DS.hasMany('App.Rsvp'),
  photos: DS.hasMany('App.Photo'),</p>

<p>  group: DS.belongsTo('App.Group'),</p>

<p>  attending_rsvps: function(){</p>

<pre><code>return Ember.ArrayController.create({
  content: this.get('rsvps').filterProperty('response','yes')
});
</code></pre>

<p>  }.property('rsvps.@each'),</p>

<p>  event_url: attr('string'),
  status: attr('string'),
  name: attr('string'),
  time: attr('date'),
  description: attr('string'),
  how_to_find_us: attr('string'),
  venue: attr('object')
})
```</p>

<p>The Event model is defined then its sync object is initialised.</p>

<p><code>js
App.Event.sync = App.meetupSync('event');
</code></p>

<p>The meetup API return an events data including the events group as a nested object but without
all the information we want to load a App.Group object into the data store.
<a id='groups'></a>
``` json events.json http://www.meetup.com/meetup_api/console/?path=/2/events</p>

<p>{
  "results": [
  {</p>

<pre><code>"status": "upcoming",
  "visibility": "public",
  "maybe_rsvp_count": 0,
  "venue": {
    "id": 6200762,
    "lon": -4.2325,
    "repinned": false,
    "name": "Eden Court",
    "address_1": "Bishops Road",
    "lat": 57.472176,
    "country": "gb",
    "city": "Inverness"
  },
  "id": "ghnjqyrgbvb",
  "utc_offset": 3600000,
  "time": 1366135200000,
  "waitlist_count": 0,
  "updated": 1331810527000,
  "yes_rsvp_count": 2,
  "created": 1296155663000,
  "event_url": "http://www.meetup.com/Highland-Web-Group/events/111106332/",
  "description": "&lt;p&gt;An informal and friendly setting for chat about all types of web-related stuff. All welcome.&lt;/p&gt;",
  "how_to_find_us": "Meet in the Tulloch Room. Ask at reception if you're unsure how to find it.",
  "name": "The Highland Web Group Monthly Meetup",
  "headcount": 0,
  "group": {
    "id": 1744559,
    "group_lat": 57.47999954223633,
    "name": "Highland Web Group",
    "group_lon": -4.230000019073486,
    "join_mode": "open",
    "urlname": "Highland-Web-Group",
    "who": "Members"
  }
</code></pre>

<p>  }]
}
<code>
A custom *munge* function is defined for handling results returned from the
API.
</code> js event.js https://github.com/HighlandWebGroup/highlandwebgroup.github.com/blob/master/app/lib/models/event.js
App.Event.sync.munge = function(json){
  if(json.group &amp;&amp; json.group.id){</p>

<pre><code>json.group_id = json.group.id;
json.group = null;
</code></pre>

<p>  }
}
```</p>

<p>The group id is extracted from the nested object and set the group_id key on the
top level event object so ember data properly handles the belongsTo
relationship. The last thing to do is set the JSON group object to null so
ember data doesn't try and materialise a App.Group from this record.</p>

<h3><a id="rsvps"></a>Rsvp Model</h3>

<p>The data returned for an rsvp is structure slightly differently to that of an Event.</p>

<p>``` json rsvps.json http://www.meetup.com/meetup_api/console/?path=/2/rsvps
{
  "results": [
  {</p>

<pre><code>"response": "yes",
  "member": {
    "name": "Blair Millen",
    "member_id": 8357569
  },
  "member_photo": {
    "photo_link": "http://photos2.meetupstatic.com/photos/member/3/b/e/f/member_5175343.jpeg",
    "highres_link": "http://photos2.meetupstatic.com/photos/member/3/b/e/f/highres_5175343.jpeg",
    "thumb_link": "http://photos2.meetupstatic.com/photos/member/3/b/e/f/thumb_5175343.jpeg",
    "photo_id": 5175343
  },
  "created": 1364297767000,
  "event": {
    "id": "ghnjqyrgbvb",
    "time": 1366135200000,
    "event_url": "http://www.meetup.com/Highland-Web-Group/events/111106332/",
    "name": "The Highland Web Group Monthly Meetup"
  },
  "mtime": 1364297767000,
  "guests": 0,
  "rsvp_id": 741139992,
  "venue": {
    "id": 6200762,
    "lon": -4.2325,
    "repinned": false,
    "name": "Eden Court",
    "address_1": "Bishops Road",
    "lat": 57.472176,
    "country": "gb",
    "city": "Inverness"
  },
  "group": {
    "id": 1744559,
    "group_lat": 57.47999954223633,
    "group_lon": -4.230000019073486,
    "join_mode": "open",
    "urlname": "Highland-Web-Group"
  }
</code></pre>

<p>  }]
}
```</p>

<p>The Rsvp model looks like this.</p>

<p>``` js rsvp.js https://github.com/HighlandWebGroup/highlandwebgroup.github.com/blob/master/app/lib/models/rsvp.js
var attr = DS.attr;</p>

<p>App.Rsvp = DS.Model.extend({</p>

<p>  event: DS.belongsTo('App.Event'),
  memberLink: function(){</p>

<pre><code>return "http://www.meetup.com/members/"+this.get('member.member_id');
</code></pre>

<p>  }.property('member'),
  comments: attr('string'),
  created: attr('date'),
  member: attr('object'),
  member_photo: attr('object'),
  response: attr('string'),</p>

<p>})</p>

<p>App.Rsvp.sync = App.meetupSync('rsvp');
```</p>

<p>The Rsvp models munge function does a little more work.</p>

<p>``` js rsvp.js https://github.com/HighlandWebGroup/highlandwebgroup.github.com/blob/master/app/lib/models/rsvp.js
App.Rsvp.sync.munge = function(json){
  if(json.rsvp_id &amp;&amp; json.rsvp_id == -1){</p>

<pre><code>//ember-data wont accept -1 id
json.id = "RecurringOrganizersRsvpId";
</code></pre>

<p>  }else{</p>

<pre><code>json.id = json.rsvp_id;
</code></pre>

<p>  }
  if(json.event){</p>

<pre><code>json.event_id = json.event.id;
json.event = null;
</code></pre>

<p>  }</p>

<p>  return json;
}
```</p>

<p>The rsvp data returned by the API uses 'rsvp_id' instead of 'id' for the object id and it also sets the rsvp_id to -1 if it a repeating event that hasn't been rsvp'd by anybody but the host. ember-data doesn't handle -1 id so we set this to something else or if an ordinary id is returned we change the key from rsvp_id to id.</p>

<p>As with the Event result data the Rsvp result data contains a nested object. In this case it's the data for the event the rsvp belongs to. We extract the events id and set the JSON Event object to null.</p>

<h2>Security</h2>

<p><a href="http://www.json-p.org">JSONP</a> or <a href="http://www.w3.org/wiki/CORS">CORS</a> can be used to make requests from different domains to the Meetup
API. The Meetup CORS requires OAuth authentication but we don't want to require
website visitors signing in to view publicly available information. JSONP on
the other hand requires an API key and because we're building a javascript app
this API key will be viewable by anybody if they look at the javascript files.
Ideally Meetup API key authentication could be tied to a specific domain that
can only be specified via their web dashboard so me as the owner of the key
could specify that it should only work for <a href="http://highlandwebgroup.github.com">highlandwebgroup.github.com</a></p>
]]></content>
  </entry>
  
</feed>
